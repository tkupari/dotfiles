#! /usr/bin/env bash
if [ -f /tmp/keyboard ]; then
  rm /tmp/keyboard
  setxkbmap -layout us
  notify-send "US layout"
else
  touch /tmp/keyboard
  setxkbmap -layout fi
  notify-send "Finnish layout"
fi


