#! /usr/bin/env bash
if [ -f /tmp/keyboard ]; then
  setxkbmap -layout fi
  notify-send "Finnish layout"
  rm /tmp/keyboard
else
  touch /tmp/keyboard
  setxkbmap -layout us
  notify-send "US layout"
fi


