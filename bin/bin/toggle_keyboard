#! /usr/bin/env bash
if [ -f ~/.config/toggle_keyboard ]; then
  rm ~/.config/toggle_keyboard
  setxkbmap -layout us
  notify-send "US layout"
else
  touch ~/.config/toggle_keyboard
  setxkbmap -layout fi
  notify-send "Finnish layout"
fi


